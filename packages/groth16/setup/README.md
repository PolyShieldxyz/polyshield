# Trusted Setup

This directory stores local development trusted setup artifacts.

Production ceremonies must use independently produced ptau/zkey files, recorded with hashes in `artifacts/manifest.json`.
