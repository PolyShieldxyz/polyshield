# Generated Verifiers

This directory is populated by:

```bash
npm run generate:verifiers
```

The generated verifier contracts are intentionally kept separate from the adapters so the adapter layer can compile and be tested independently of local artifact generation.
