.claude/
.devcontainer/
# Private working docs at repo root (tracked versions live in docs/)
/CONTRIBUTING.md

Benchmarking/

marketing/

# Generated backend env (contains Anvil keys — regenerated by pnpm dev:mock)
packages/.env.test

.env
.env.local
.env.development.local
.env.test.local
.env.production.local


# Mainnet deploy secrets (live private keys — NEVER commit; the .env.mainnet.example template IS tracked)
.env.mainnet
/deploy/.env
/deploy/env/common.env
/deploy/env/frontend.env
/deploy/env/proof-relay.env
/deploy/env/signing-layer.env
/deploy/cloudflare-dns.sh

# Dependencies
node_modules/
.pnpm-store/

/docs/blog-design.md
/docs/collateral-deployment-strategy-comparison.md
/docs/blog-content-system.md

# Noir build artifacts
packages/circuits/**/target/
# Noir spec circuits now live under packages/circuits/Noir/ and are tracked as a
# specification reference; only their build outputs (target/, bench_out/) are ignored.
packages/circuits/Noir/**/bench_out/


# Foundry artifacts
packages/contracts/out/
packages/contracts/cache/

# Recursive-submodule deps of the vendored OZ-upgradeable tree (build uses the
# top-level lib/openzeppelin-contracts + lib/forge-std via remappings — these are redundant)
packages/contracts/lib/openzeppelin-contracts-upgradeable/lib/
packages/contracts/MockAcceptVerifier.s.sol
packages/contracts/MockDeploy.s.sol

# TypeScript build
packages/backend/**/dist/
packages/backend/mock-clob-server/
packages/backend/mock-env/

packages/backend/beta-consent.db
packages/backend/beta-consent.db-shm
packages/backend/beta-consent.db-wal
packages/backend/catalog.db
packages/backend/catalog.db-shm
packages/backend/catalog.db-wal
packages/backend/settlement.db

# Next.js build cache
packages/frontend/.next/
packages/frontend/node_modules/
packages/frontend/design-system/
# TypeScript incremental build cache (regenerated on every build; churns constantly)
packages/frontend/*.tsbuildinfo

# Frontend API route handlers — PolyShield does not publish a public API. These server
# routes document the (otherwise loopback-only) backend surface, so they are kept LOCALLY
# for builds but never tracked on GitHub. NOTE: a fresh `git clone` will NOT contain these;
# build/deploy from a working tree that has them on disk (see deploy runbook).
packages/frontend/src/app/api/

# Proving keys — large binary files, regenerate via pnpm circuits:deploy
packages/frontend/public/zkeys/
# Circuit WASM files — regenerate via pnpm circuits:deploy
packages/frontend/public/circuits/*.wasm
# Circuit build JSON (legacy Noir ACIR / circuit info — NOT used by the snarkjs prover,
# which loads only .wasm + .zkey). Regenerated by the circuit pipeline; never needed in git.
packages/frontend/public/circuits/*.json

# Key material — defensive belt-and-suspenders so no private key/keystore is ever committed
*.pem
*.keystore
*keystore*.json
*-private.json
*.secret

# Misc
.DS_Store
*.log

# Session logs (generated by pnpm dev:mock and frontend logger)
logs/

# Foundry broadcast dev-run files (timestamped; only run-latest.json is kept)
packages/contracts/broadcast/**/run-[0-9]*.json

# Circuit benchmarking outputs
packages/circuits/bench_out/

# SQLite databases (runtime state — regenerated by services, never commit)
packages/backend/*.db
packages/backend/*.db-shm
packages/backend/*.db-wal
packages/backend/indexer/data/
packages/backend/data/

packages/test-fixtures/node_modules/

packages/backend/node_modules/
packages/backend/dist/
packages/backend/cache/
packages/backend/out/
packages/backend/proof-relay/node_modules/
packages/backend/signing-layer/node_modules/
packages/backend/signing-layer/dist/
packages/backend/signing-layer/cache/
packages/backend/signing-layer/out/